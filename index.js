$("#tabhome a").click(
	function (event) {
		event.preventDefault();
		$(this).tab("show");
	}
);

$("#tabprof a").click(
	function (event) {
		event.preventDefault();
		$(this).tab("show");
	}
);

$("#tabmsgs a").click(
	function (event) {
		event.preventDefault();
		$(this).tab("show");
	}
);